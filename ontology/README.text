This is a collection of queries that query aspects of the Wikidata ontology.

These queries are for subclass closure and instance.

queries.tsv is a control file for use with the benchmark.py benchmark control program