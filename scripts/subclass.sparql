# curl -Gs http://getafix.local:7001 --header 'Accept:text/tab-separated-values' --data-urlencode query@<file>
PREFIX wdt: <http://www.wikidata.org/prop/direct/>
SELECT (COUNT (*) AS ?c) WHERE {
  SELECT DISTINCT ?s ?a WHERE { ?s wdt:P279+ ?a . }
  }
